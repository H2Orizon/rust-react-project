{
  "devDependencies": {
    "concurrently": "^9.2.1"
  },
  "scripts": {
    "dev":  "concurrently \"npm run backend\" \"npm run frontend\"",
    "backend": "cd backend && cargo run",
    "frontend": "cd frontend && npm run dev",
    "migration-refresh": "cd backend/database/migration && cargo run -- fresh"
  }
}
